class_name GenericSchema
extends DatabaseSchema

## The default, database-agnostic schema. Collection grouping uses the base
## DatabaseSchema heuristics (shared leading words, basic plural folding) with no
## product-specific rules.
